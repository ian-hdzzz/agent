"""
{{ infra.display_name or infra.name }} - Hive Worker Configuration ({{ agent.display_name or agent.agent_id_slug }})
Generated by PACO Infrastructure Manager
"""

import os
from functools import lru_cache

from pydantic_settings import BaseSettings


class WorkerSettings(BaseSettings):
    worker_id: str = os.getenv("WORKER_ID", "{{ agent.agent_id_slug }}")
    agent_slug: str = os.getenv("AGENT_SLUG", "{{ agent.agent_id_slug }}")
    coordinator_url: str = os.getenv("COORDINATOR_URL", "http://localhost:{{ infra.port_range_start }}")
    poll_interval: int = int(os.getenv("POLL_INTERVAL", "5"))

    anthropic_api_key: str = os.getenv("ANTHROPIC_API_KEY", "")
    database_url: str = os.getenv("DATABASE_URL", "postgresql+asyncpg://postgres:postgres@localhost:5432/{{ infra.db_name or infra.name }}")
    redis_url: str = os.getenv("REDIS_URL", "redis://localhost:6379")
    log_level: str = os.getenv("LOG_LEVEL", "INFO")

    host: str = "0.0.0.0"
    port: int = 8000

    class Config:
        env_file = ".env"
        case_sensitive = False


@lru_cache()
def get_settings() -> WorkerSettings:
    return WorkerSettings()
