module.exports = {
  apps: [{
    name: '{{ agent.name }}',
    script: 'dist/server.js',
    env: {
      NODE_ENV: 'production',
      PORT: {{ agent.port }},
{% for key, value in env_vars.items() %}
      {{ key }}: '{{ value }}',
{% endfor %}
    },
    max_memory_restart: '500M',
    instances: 1,
    autorestart: true,
    watch: false,
  }],
};
