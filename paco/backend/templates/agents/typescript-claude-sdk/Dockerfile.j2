FROM node:22-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci --production=false

COPY . .
RUN npm run build

ENV NODE_ENV=production
ENV PORT={{ agent.port }}

EXPOSE {{ agent.port }}

CMD ["node", "dist/server.js"]
