---
phase: 02-canvas-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/builder/layout.tsx
  - frontend/app/builder/page.tsx
  - frontend/components/ui/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "User must be logged in to access /builder route"
    - "Builder link appears in sidebar navigation with correct active state"
    - "Canvas fills available viewport after sidebar offset"
    - "All Phase 1 canvas functionality still works (drag, drop, pan, zoom, undo/redo)"
  artifacts:
    - path: "frontend/app/builder/layout.tsx"
      provides: "Authenticated layout with sidebar"
      contains: "useAuth"
    - path: "frontend/components/ui/Sidebar.tsx"
      provides: "Builder navigation link"
      contains: "Builder"
  key_links:
    - from: "frontend/app/builder/layout.tsx"
      to: "@/lib/auth"
      via: "useAuth hook"
      pattern: "useAuth.*token"
    - from: "frontend/app/builder/layout.tsx"
      to: "@/components/ui/Sidebar"
      via: "import and render"
      pattern: "<Sidebar"
---

<objective>
Integrate the builder canvas with PACO's authenticated layout system

Purpose: The builder route currently bypasses PACO's standard layout (sidebar + auth). Users need consistent navigation and must be authenticated to access the builder.

Output: Builder accessible via sidebar link, protected by authentication, canvas fills available space correctly.
</objective>

<execution_context>
@/Users/fernandocamacholombardo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fernandocamacholombardo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-canvas-integration/02-RESEARCH.md

# Phase 1 established the builder components
@frontend/app/builder/layout.tsx
@frontend/app/builder/page.tsx
@frontend/components/ui/Sidebar.tsx

# Reference authenticated layout pattern
@frontend/app/dashboard/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update builder layout with authentication and sidebar</name>
  <files>frontend/app/builder/layout.tsx</files>
  <action>
Update the builder layout to match PACO's authenticated layout pattern from dashboard/layout.tsx:

1. Add "use client" directive at the top
2. Import useEffect from react, useRouter from next/navigation
3. Import useAuth from @/lib/auth
4. Import Sidebar from @/components/ui/Sidebar
5. Add auth check pattern:
   - Get token and refreshUser from useAuth()
   - useEffect that redirects to /auth/login if no token
   - useEffect that calls refreshUser() when token exists
   - Return null if no token (prevents flash)
6. Render structure:
   - Outer div: min-h-screen bg-background
   - Sidebar component
   - main with className="pl-64 h-screen overflow-hidden"
   - Inner div with "flex flex-col h-full" for canvas container

Key: The h-screen on main prevents canvas from overflowing while pl-64 accounts for the sidebar width.
  </action>
  <verify>
Run `npm run build` in frontend/ directory - should compile without errors.
Visit http://localhost:3006/builder without auth - should redirect to /auth/login.
  </verify>
  <done>
Builder layout matches dashboard layout pattern with auth check and sidebar.
Unauthenticated users redirected to login.
Canvas has correct height constraints for sidebar offset.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Builder link to sidebar navigation</name>
  <files>frontend/components/ui/Sidebar.tsx</files>
  <action>
Add the Builder navigation link to the sidebar:

1. Import the Workflow icon from lucide-react (add to existing icon imports)
2. Add to the navigation array, positioned after Dashboard and before Agents:
   { name: "Builder", href: "/builder", icon: Workflow }

The existing isActive logic will automatically handle the active state because it uses:
pathname === item.href || pathname.startsWith(item.href + "/")

No other changes needed - the existing Sidebar component handles everything.
  </action>
  <verify>
Visit http://localhost:3006/builder (while authenticated).
Verify "Builder" appears in sidebar between "Dashboard" and "Agents".
Verify the Builder link shows active state (highlighted) when on /builder route.
  </verify>
  <done>
Builder link visible in sidebar navigation.
Active state works correctly when on /builder route.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify canvas functionality still works</name>
  <files>frontend/app/builder/page.tsx</files>
  <action>
Verify the builder page still renders correctly with the new layout. The page.tsx should not need changes, but verify:

1. The TeamBuilder component receives the correct props (team, onChange, etc.)
2. The outer div uses "h-full w-full" (not h-screen, since parent controls height now)

If page.tsx currently uses h-screen on its outer div, change it to h-full since the layout now handles the viewport height.

Run the frontend dev server and test:
- Canvas renders and fills available space
- Drag-drop from library works
- Pan/zoom works (mouse wheel, trackpad)
- Node selection works
- Undo/redo works (toolbar buttons)
  </action>
  <verify>
Run `npm run dev` in frontend/ and navigate to /builder.
Visually confirm:
1. Sidebar appears on left
2. Canvas fills remaining space
3. Drag an Agent from library to canvas - node appears
4. Use scroll wheel to zoom - canvas zooms
5. Click undo button - node removed
6. Click redo button - node returns
  </verify>
  <done>
All Phase 1 canvas functionality confirmed working with new layout.
CANVAS-01 through CANVAS-06 requirements verified functional.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Authentication**: Access /builder without token -> redirects to /auth/login
2. **Navigation**: Builder link visible in sidebar, shows active state
3. **Layout**: Canvas fills available space, no scrollbars on container
4. **Functionality**: All canvas interactions work:
   - Drag from palette (CANVAS-01)
   - Connect nodes with edges (CANVAS-02)
   - Pan and zoom (CANVAS-03)
   - Select node shows properties panel (CANVAS-04)
   - Delete selected node (CANVAS-05)
   - Undo/redo (CANVAS-06)

Build verification: `npm run build` passes with no TypeScript errors.
</verification>

<success_criteria>
- [ ] Builder route requires authentication (redirects if no token)
- [ ] Builder link appears in sidebar navigation
- [ ] Builder link shows active state when on /builder
- [ ] Canvas fills available viewport correctly (sidebar + canvas)
- [ ] All 6 CANVAS requirements remain functional
- [ ] npm run build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-canvas-integration/02-01-SUMMARY.md`
</output>
